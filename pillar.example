# vim: ft=yaml

yum:
  # Lookup static data
  lookup:
    config: /etc/yum/yum.conf

  # Global configuration options, see man 5 yum.org
  options:
    keepcache: 1
    proxy: http://proxy.example.net:3128
